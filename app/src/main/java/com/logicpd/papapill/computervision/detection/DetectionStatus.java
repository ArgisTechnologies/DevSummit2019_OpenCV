package com.logicpd.papapill.computervision.detection;

/*
 * This enum is the basic statuses that detection can deliver to the rest of the system.
 */
public enum DetectionStatus {
    SUCCESSFUL,
    ABORTED,
    FAILED,
}
